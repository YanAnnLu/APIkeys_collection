# APIkeys Collection GTD

Last updated: 2026-05-17

## Product GTD

| Area | Status | Current Progress | Next Step |
| --- | --- | --- | --- |
| Product architecture | In progress | Architecture documented in `docs/ARCHITECTURE.md`; pipeline, runtime layers, and path resolver are explicit. | Keep architecture current as UI/download/database workflows are connected. |
| Product positioning | MVP | `docs/PRODUCT_POSITIONING.zh-TW.md` reframes the project as a Steam-like scientific dataset launcher plus virtual twin data pipeline, not just an API key/source manager. | Keep positioning aligned as renderer and database installer workflows mature. |
| Folder hygiene | MVP | Docs, catalog/reference files, example config, scripts, and Tk frontend implementation moved into folders; Tk implementation renamed to `frontends/tk/launcher_ui.py` while root wrappers keep legacy entry points compatible. | Move remaining runtime files into ignored `state/` after compatibility checks and create `frontends/unreal/` when Unreal tooling becomes tracked code. |
| Provider catalog | MVP | Built-in provider list, manual add/edit, categories, metadata fields, source discovery seeds, duplicate-aware provider model. | Improve provider-source separation so one dataset can have multiple resource sites. |
| Provider/source discovery | MVP | Discovery seed config and CLI collect candidate metadata from source sites without scraping secrets. Built-in discovery seeds expanded from 8 to 30 source sites across climate, ocean, biodiversity, statistics, maps, research metadata, and Taiwan open data. | Add UI search/import flow for discovered provider candidates and periodic seed review. |
| Dataset adapters | Skeleton | Adapter registry exists; HYG v3.8 star catalog and GEBCO 2025 elevation grid can be discovered into the dataset table from their providers. | Add NOAA/NASA and ERDDAP dataset adapters with download manifests. |
| Dataset freshness/versioning | Skeleton | Generic version option model exists; adapters can expose `available_versions`; UI right-click can add a provider with a specific dataset version to the download plan. GEBCO 2025 is marked compatibility-pinned while GEBCO 2026 requires review. | Persist multiple versions of the same dataset in one plan and add stale/compatibility-pinned UI badges. |
| Dataset transition/delta planning | Skeleton | Generic transition planner exists for install-new, same-version skip, upgrade, downgrade, partial forward/backward, compare-then-update, and keep-legacy-side-by-side decisions; adapter version metadata can declare update strategy and dedupe keys. | Implement provider manifests/checksums, row-level/table-level dedupe, rollback-safe installs, and incremental update adapters where providers support deltas. |
| Steam-like UI | In progress | Responsive Tk UI, category sidebar, simplified source table, proportional right detail drawer, star/pin, Excel-like manual column resizing with persisted widths, double-click to add to plan, native menu bar for File/Library/Integrations/Tools/Help, compact toolbar with More menu, basic download job controls, and more localized detail/download labels. | Make download plan collapsible, polish visual hierarchy, and replace remaining mojibake labels. |
| Steam-like library actions | MVP | `api_launcher/library_actions.py` defines shared action availability rules for add-to-plan, install, update, repair, open database, render preview, and guarded uninstall; it exposes action map/order/menu-label helpers used by the Tk right-click menu, plus an agent-readable JSON payload available through `--show-library-actions ... --library-actions-json`. | Add richer action reasons/status badges and connect enabled actions to repair/retry suggestions. |
| Download plan/cart | MVP | Download Plan panel, add/remove/clear/export plan JSON, double-click source row to add, plan schema declares nonblocking download policy, Start/Pause/Resume/Cancel controls exist, source rows show Direct/Adapter/Docs eligibility. | Add provider-specific adapters for non-direct sources. |
| Download engine | MVP | Nonblocking worker queue, progress snapshots, pause/resume/cancel/retry, tests, pluggable transfer tools, HTTP(S) adapter with `.part` resume, configurable polite per-host pacing, 429/503 cooldown, UI job table, completion registry update. | Add provider-specific adapters and expose rate-limit settings in UI. |
| Download staging/manifests | Skeleton | HTTP downloads now stage payloads under `state/staging/`, promote atomically to final downloads, write sidecar JSON manifests, and register manifests in SQLite with size/checksum/version/source metadata. Legacy `.part` files are migrated into staging for resume. | Use manifest registry for update/dedupe decisions. |
| Download repair scanner | MVP | CLI `--verify-downloads`, `--manifest-health`, `--list-manifests`, and `api_launcher/repair.py` can scan sidecar manifests, detect missing payloads/size mismatch/checksum mismatch/manifest parse errors, sync health status into SQLite, and Tk UI now has a localized Repair / verify assets panel with file-manifest details plus safe requeue suggestions. HTTP(S) manifests with `source_url` and `provider_id` can be requeued through staging from the panel. | Expand repair suggestions to adapter-specific datasets, richer logs, and agent-readable repair summaries. |
| External transfer tools | MVP | `python_internal`, `aria2c`, and `curl` profiles exist; commands are built as cross-platform argument lists. | Add runtime selection UI and optional `aria2c` install guidance. |
| Database client interface | MVP | Local integration config, active database client selection, UI settings dialog, open/test configured client. | Add custom profile creation/editing directly in UI instead of editing JSON. |
| Data store connections | Skeleton | `data_store_connections.py` is the single connection-profile contract for relational SQL, embedded SQLite, MongoDB/document stores, S3-compatible object storage, and vector DBs; it now includes a connection-test result contract, SQLite read-only introspection, and optional-driver probes for MySQL/PostgreSQL. CLI `--test-data-store PROFILE_ID|all` and the Tk Data store dialog can run the checks without storing secrets. | Extend tests into schema/table introspection, drift detection, and safe install/uninstall adapters across SQL and non-SQL stores. |
| SQL/database self-check | MVP | SQLite file existence/read-only connection/table-count checks work through the data-store tester; `--self-check-databases` now verifies managed SQLite database/table assets and MySQL/PostgreSQL database/table assets from the install registry. SQLite uses read-only path checks; MySQL/PostgreSQL use env/driver probes plus `information_schema` for table counts, table existence, and optional database/table schema fingerprints. Table assets carry install ownership through `install_location`, and PostgreSQL table names may use `schema.table`. Missing/error details now include stable repair suggestions; `--self-check-databases-json` emits agent-readable issue payloads; Tk Repair / verify assets has a Databases tab that shows localized repair guidance without destructive SQL. | Add configurable SQL profile/schema selection per asset, real-driver integration smoke coverage, and turn suggestions into guarded adapter-owned repair actions. |
| Install registry | Skeleton | Install IDs, fingerprints, asset roles, provenance metadata, managed/unmanaged states. | Connect real downloaded/imported assets to install records automatically. |
| Uninstall/delete database | Skeleton | Safe uninstall metadata exists; current flow marks registry assets removed, not destructive SQL. | Implement guarded SQL `DROP DATABASE` / file delete only for verified install IDs. |
| Data cleaning pipeline | Planned | Need acknowledged for API/CSV/JSON/manual SQL normalization. | Define adapter contract for raw -> curated validation, schema fingerprinting, and error reports. |
| Manual import support | Planned | Product requirement defined for CSV/JSON/manual SQL imports. | Add import wizard and provenance rules for user-provided files/tables. |
| AI summary | MVP | Local Ollama and optional Gemini API-key profile support can generate provider descriptions; CLI `--generate-ai-summary PROVIDER --ai-profile gemini_flash` exists; `ai_prompts.py` defines the dataset-description prompt contract; Tk UI can enable Gemini for the current session and has QR/device-login skeleton. | Implement real Google OAuth device-code flow, QR rendering, secure token vault, per-provider prompt controls, and cached summaries. |
| Pipeline documentation | MVP | Architecture and Chinese technical overview now include the full Steam-like launcher -> download/import -> governance -> renderer bridge -> Taichi/Unreal/agent pipeline as Mermaid diagrams. | Keep diagram updated whenever a major module moves from skeleton to MVP. |
| Tile/cache manifest | Skeleton | `api_launcher/tile_manifests.py` defines `TileManifest`, `TileAsset`, `GeoBounds`, JSON round-trip, stable tile IDs, global grid manifest generation, CLI `--write-tile-manifest`, and Unreal preview `TileManifest.json` output. | Connect GEBCO/HYG adapters and install registry records to generated tile manifests. |
| Simulation bridge | Skeleton | `api_launcher/simulation_bridge.py` defines contract-only water and air-quality simulation inputs/backends so datasets provide boundary conditions without pretending physics is already implemented. | Evaluate water simulation options: Unreal Water/Niagara, Gerstner/FFT, shallow-water approximation, flow maps, and Taichi preview kernels. |
| Renderer bridge | Skeleton | Renderer contracts, GEBCO/HYG dataset IDs, copied `taichi_global_bathymetry.py`, renderer requirements documented; HYG and GEBCO discovery now use shared renderer contract IDs; Chinese frontend note defines Taichi as a cross-platform GPU/CPU reference renderer inspired by the Taipei renderer profile/cache approach. | Map installed dataset IDs to renderer-ready file paths and shared tile/cache manifests usable by Taichi and Unreal. |
| Unreal Engine bridge | Skeleton | Local UE 5.7 install detected; local `APIkeysVirtualTwin.uproject` created outside repo; preview exporter writes GEBCO/HYG-derived OBJ/MTL/CSV plus bridge/tile manifests into `Content/APIkeysCollection/Preview`; `frontends/unreal/` now documents the Unreal frontend boundary. | Add Unreal-side auto-import/editor utility, then implement tile manifest reader and runtime streamer interface. |
| AI-ready dataset metadata | Planned | Product requirement defined: catalog entries should eventually state license, usage policy, training suitability, attribution, and redistribution limits. | Extend provider/dataset schema with license and AI training suitability fields before using downloaded data for local LLM workflows. |
| Taichi renderer health | Planned | Renderer copied into project but not refactored or performance-tested in this cycle. | Add smoke tests/config checks without forcing heavy renderer dependencies into launcher. |
| Cross-platform setup | MVP | Git/GitHub, GitHub CLI (`gh`) for CI log inspection, Docker, PowerShell/bash runners, GitHub Actions CI for Windows/Ubuntu, Chinese Windows/macOS/Linux setup notes, example/local config split, UTF-8/LF rules, startup environment checks, and Windows SQLite file-handle guidance. | Add CI coverage for renderer-light smoke checks and consider static checks for unclosed SQLite probes. |
| Error logs / observability | MVP | Structured JSONL event log and human-readable error log exist under `state/logs/`; UI and CLI start recording startup/download/crawl/AI/database failures; CLI `--show-logs N` prints recent events; Tk UI now has `Tools > Recent event logs` and a manifest repair panel for handoff/debug inspection. | Add richer agent-readable failure summaries and connect log entries to repair/retry suggestions. |
| Agent handoff report | Skeleton | CLI `--handoff-report PATH` writes a Markdown handoff with Git state, catalog counts, manifest health, recent logs, and resume checks; `docs/AGENT_HANDOFF.zh-TW.md` is now the fixed cross-Agent handoff card. | Include open GTD items and last verification timestamps, and keep the handoff card updated before switching machines. |
| Failure recovery | Planned | Failure-mode document exists for network, user actions, manual deletion, SQLite locks, version transitions, path changes, provider URL drift, and cross-platform local path mistakes. Startup checks now detect foreign platform paths such as Windows `K:\...` on macOS before `pathlib` resolves them. | Implement staging/promote, manifest verification, rollback-safe updates, and path repair workflow. |
| Agent skill packaging | Skeleton | Local `.codex/skills/apikeys-collection-launcher` draft exists and now documents the CLI path for reusing shared library-action policy instead of duplicating install/update/repair logic; it includes repair/download/database self-check notes and warns agents to preserve unexpected uncommitted changes before restore/delete decisions. | Expand the skill with adapter-specific repair flows and MySQL/PostgreSQL self-check guidance. |
| Documentation | In progress | `docs/DOCS_INDEX.zh-TW.md` now defines reading order and cleanup plan; current docs preserve architecture/product decisions but are becoming too numerous. | Consolidate docs into a few primary files plus appendices; avoid duplicate pipeline explanations. |

## Status Legend

| Status | Meaning |
| --- | --- |
| Planned | Requirement is known, but implementation has not started. |
| Skeleton | Core shape exists, but not enough for normal user workflow. |
| MVP | Works in a narrow tested path and can be built upon. |
| In progress | Partially usable, active design/implementation still changing. |
| Done | Stable enough that only maintenance is expected. |
