services:
  launcher:
    build: .
    image: apikeys-collection:dev
    working_dir: /app
    volumes:
      - .:/app
      - apikeys_collection_state:/state
    environment:
      APIKEYS_COLLECTION_STATE_DIR: /state
    command: python APIkeys_collection.py --summary

volumes:
  apikeys_collection_state:
